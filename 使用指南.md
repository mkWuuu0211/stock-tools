# 📊 股票形态相似性选股工具 - 使用指南

## 目录
1. [快速开始](#快速开始)
2. [数据同步](#数据同步)
3. [形态匹配功能](#形态匹配功能)
4. [命令行脚本](#命令行脚本)
5. [常见问题](#常见问题)

---

## 快速开始

### 1. 环境要求

- Python 3.8+
- 约 5-10GB 磁盘空间（存储全市场日线数据）
- 推荐 8GB 以上内存

### 2. 首次运行步骤

#### 步骤1：安装依赖
```bash
cd stock-tools
pip install -r requirements.txt
```

#### 步骤2：配置环境变量（可选）
复制 `.env.example` 到 `.env` 并配置（如果有Tushare积分）：
```bash
# .env
TUSHARE_TOKEN=your_tushare_token_here
```

> 💡 **说明**：如果没有Tushare Token，系统会自动降级使用免费数据源，也可以正常使用。

#### 步骤3：启动Web应用
```bash
streamlit run web/app.py
```

启动后浏览器会自动打开，或手动访问：http://localhost:8515

---

## 数据同步

首次使用必须先同步股票历史数据。

### ✨ 后台同步特性

- **不中断同步**：同步在后台独立进程运行，刷新页面或关闭浏览器都不会中断
- **进度持久化**：同步进度实时保存到数据库，任何时候访问页面都能看到当前进度
- **自动刷新**：页面每3秒自动刷新更新进度（可手动关闭）

### 方式一：通过Web界面同步（推荐）

1. 启动应用后，在左侧导航栏点击 **📥 数据管理**
2. 选择时间周期：
   - `D 日线` - 推荐，全市场约5000+股票，约500MB
   - `W 周线` / `M 月线` - 从日线自动合成，无需单独下载
3. 可以设置「限制同步数量」用于快速测试
4. 点击 **🔄 开始同步**
5. ✅ **可以随时刷新页面**，进度会在页面底部持续显示

### 方式二：通过命令行脚本同步

```bash
# 同步日线数据（推荐）
python scripts/sync_all_data.py --freqs D

# 同步多个周期
python scripts/sync_all_data.py --freqs D,W

# 只同步前100只股票（快速测试）
python scripts/sync_all_data.py --freqs D --limit 100
```

### 每日增量更新

收盘后运行增量更新脚本：
```bash
python scripts/update_daily.py
```

---

## 📈 K线图可视化

查看单只股票的K线图和成交量。

### 使用步骤

1. 在左侧导航栏点击 **📈 K线图展示**
2. **选择股票**：在下拉框中搜索或选择股票
3. **选择时间周期**：日线/周线/月线/季线/年线
4. **调整显示范围**：
   - 用滑块选择显示最近N条K线
   - 或勾选「显示全部」查看完整历史

### 功能说明

- **蜡烛图**：标准OHLC K线显示，红涨绿跌
- **成交量副图**：柱形图显示每日成交量
- **均线切换**：可开关显示 MA5/MA10/MA20/MA60 均线
- **数据表格**：可展开查看原始行情数据

---

## 形态匹配功能

### 使用步骤

1. 在左侧导航栏点击 **🔍 形态相似性选股**

2. **选择目标股票**
   - 在下拉框中搜索或选择股票（如：000001 平安银行）

3. **选择时间周期**
   - `D` - 日线（推荐）
   - `W` - 周线
   - `M` - 月线
   - `15min/30min/60min` - 分钟线

4. **调整高级参数（可选）**
   - **返回Top N结果** - 返回最相似的前N只股票（默认20）
   - **使用多进程加速** - 启用后利用多核CPU，匹配速度提升3-5倍

5. **选择目标形态区间**
   - 用滑块选择K线图的起始和结束位置
   - 建议选择有明显形态特征的区间（如上涨趋势、盘整、V型反转等）
   - 区间长度建议：10-60根K线

6. **开始匹配**
   - 点击 **🚀 开始匹配**
   - 等待计算完成（全市场约 10-30 秒）

### 结果解读

#### 匹配结果表格
| 列名 | 说明 |
|------|------|
| 排名 | 相似度排名 |
| 股票代码 | 匹配到的股票代码 |
| 名称 | 股票名称 |
| 相似度 | 综合相似度得分 [0,1]，越接近1越相似 |
| 起始位置 | 匹配形态在候选股票K线中的起始索引 |

#### 对比图表
- 点击每只股票展开对比图表
- 上方：你选择的目标形态
- 下方：匹配到的相似形态
- 两者都经过归一化处理，便于视觉对比

---

## 命令行脚本

### 1. 全量数据同步 `scripts/sync_all_data.py`

**参数说明：**
```bash
--freqs FREQ1,FREQ2   # 要同步的周期，逗号分隔
                       # 可选值: D, W, M, 1min, 5min, 15min, 30min, 60min
--limit N              # 只同步前N只股票（用于快速测试）
--start-date YYYYMMDD  # 开始日期（默认20100101）
```

**使用示例：**
```bash
# 同步所有日线数据
python scripts/sync_all_data.py --freqs D

# 同步日线和周线
python scripts/sync_all_data.py --freqs D,W

# 快速测试：同步前100只股票日线
python scripts/sync_all_data.py --freqs D --limit 100
```

### 2. 每日增量更新 `scripts/update_daily.py`

```bash
# 更新所有股票最新日线数据
python scripts/update_daily.py
```

输出示例：
```
==================================================
Daily Update Summary:
==================================================
  Success: 5234
  Failed:  2
==================================================
```

---

## 常见问题

### Q1: 启动时报错 "ModuleNotFoundError"
**A**: 确保已安装所有依赖：
```bash
pip install -r requirements.txt
```

### Q2: 股票列表为空 / 看不到股票可选
**A**: 需要先同步股票列表和数据：
1. 进入「数据管理」页面
2. 点击同步按钮
3. 刷新页面

### Q3: 匹配速度很慢
**A**:
1. 确保勾选了「使用多进程加速」选项
2. CPU核心数越多越快
3. 减少「返回Top N结果」数量可以加速

### Q4: 找不到匹配结果 / 相似度都很低
**A**:
1. 检查是否已同步足够多的股票数据
2. 尝试选择更长的形态区间（20-60根K线）
3. 降低皮尔逊阈值（在代码中调整 settings.py）

### Q5: 数据文件存储在哪里
**A**: 数据默认存储在项目下的 `data/` 目录：
```
data/
├── stock_metadata.db   # SQLite元数据库
├── bars/               # 行情数据（Parquet格式）
│   ├── D/              # 日线
│   ├── W/              # 周线
│   ├── M/              # 月线
│   └── ...
```

### Q6: 如何清除数据重新同步
**A**: 直接删除 `data/` 目录即可：
```bash
rm -rf data/
```

### Q7: 周线/月线数据如何获取
**A**: 不需要单独同步周线/月线。系统会自动从日线数据合成周线和月线，节省存储空间和同步时间。

### Q8: 分钟线数据怎么用
**A**: 分钟线需要Tushare积分才能获取：
1. 配置 TUSHARE_TOKEN
2. 同步分钟线数据：`--freqs 15min,30min,60min`
3. 在Web界面选择对应周期

### Q9: 刷新页面会中断数据同步吗
**A**: 不会。数据同步在后台独立进程中运行，你可以：
- 安全地刷新页面
- 关闭浏览器后重新打开
- 切换到其他页面操作

同步进度会实时保存到数据库，下次访问时自动恢复显示。

### Q10: 如何关闭自动刷新
**A**: 在「数据管理」页面顶部取消勾选「自动刷新进度」即可。

---

## 高级配置

修改 `config/settings.py` 调整算法参数：

```python
# 匹配算法参数
PEARSON_THRESHOLD = 0.5      # 一级过滤阈值，越低通过的越多
MAX_CANDIDATES_AFTER_FIRST = 500  # 一级过滤后保留候选数
MAX_CANDIDATES_AFTER_SECOND = 100  # 二级精配后保留候选数

# 权重配置
WEIGHT_PEARSON = 0.4        # 皮尔逊相关系数权重
WEIGHT_DTW = 0.4            # DTW距离权重
WEIGHT_FEATURE = 0.2        # 形态特征权重

# 多进程
DEFAULT_N_PROCESSES = 4     # 默认进程数
```

---

## 项目结构

```
stock-tools/
├── web/                     # Streamlit Web界面
│   ├── app.py              # 主入口
│   └── pages/              # 子页面
│       ├── 1_📥_数据管理.py       # 数据管理（后台同步）
│       ├── 2_🔍_形态相似性选股.py  # 形态匹配
│       └── 3_📈_K线图展示.py      # K线可视化
├── src/                     # 核心代码
│   ├── common/              # 公共模块
│   │   └── data/           # 数据层
│   └── pattern_matcher/    # 形态匹配模块
│       ├── algorithm/      # 核心算法
│       └── service/        # 业务服务
├── scripts/                 # 工具脚本
│   ├── sync_all_data.py    # 全量同步
│   └── update_daily.py     # 每日更新
├── config/                  # 配置
├── data/                    # 数据存储（自动创建）
├── tests/                   # 单元测试
├── requirements.txt
└── 使用指南.md              # 本文件
```
